name: test

on:
  push:
  pull_request:

jobs:
  self-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run the suite under stock /bin/bash
        run: /bin/bash ./bin/ai-usage --self-test
